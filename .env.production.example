# -----------------------------------------------------------------------------
# Web Information
# -----------------------------------------------------------------------------
NEXT_PUBLIC_WEB_URL = "https://your-domain"
NEXT_PUBLIC_PROJECT_NAME = "3D-MARKER"

# -----------------------------------------------------------------------------
# Database with Supabase
# -----------------------------------------------------------------------------
# Backend envs used by server code/scripts
SUPABASE_URL = ""
SUPABASE_ANON_KEY = ""
SUPABASE_SERVICE_ROLE_KEY = ""

# Public envs for frontend usage (optional)
NEXT_PUBLIC_SUPABASE_URL = ""
NEXT_PUBLIC_SUPABASE_ANON_KEY = ""

# -----------------------------------------------------------------------------
# Auth with next-auth
# -----------------------------------------------------------------------------
AUTH_SECRET = ""

# Google Auth
AUTH_GOOGLE_ID = ""
AUTH_GOOGLE_SECRET = ""
NEXT_PUBLIC_AUTH_GOOGLE_ID = ""
NEXT_PUBLIC_AUTH_GOOGLE_ENABLED = "true"
NEXT_PUBLIC_AUTH_GOOGLE_ONE_TAP_ENABLED = "false"

# -----------------------------------------------------------------------------
# Payment with Stripe
# -----------------------------------------------------------------------------
STRIPE_PUBLIC_KEY = ""
STRIPE_PRIVATE_KEY = ""
STRIPE_WEBHOOK_SECRET = ""

NEXT_PUBLIC_PAY_SUCCESS_URL = "https://your-domain/my-orders"
NEXT_PUBLIC_PAY_FAIL_URL = "https://your-domain/#pricing"
NEXT_PUBLIC_PAY_CANCEL_URL = "https://your-domain/#pricing"

ADMIN_EMAILS = "admin@your-domain"
NEXT_PUBLIC_DEFAULT_THEME = "light"

# -----------------------------------------------------------------------------
# Storage with aws s3 sdk (e.g. Cloudflare R2)
# -----------------------------------------------------------------------------
STORAGE_ENDPOINT = ""
STORAGE_REGION = "auto"
STORAGE_ACCESS_KEY = ""
STORAGE_SECRET_KEY = ""
STORAGE_BUCKET = ""
STORAGE_DOMAIN = ""  # optional CDN domain

# Download mode for assets (Step-07)
# - presigned: generate a short-lived signed URL (302 redirect by default; support 200 JSON via ?format=json)
# - proxy:     stream file via server (no public URL; each access requires auth)
STORAGE_DOWNLOAD_MODE = "presigned"

# TTL of presigned URL in seconds (default 300). Only takes effect when STORAGE_DOWNLOAD_MODE=presigned
STORAGE_SIGN_URL_TTL_SEC = "300"

# -----------------------------------------------------------------------------
# Hitem3D API
# -----------------------------------------------------------------------------
HITEM3D_API_BASE = "https://api.hitem3d.ai"
HITEM3D_CALLBACK_URL = "https://your-domain/api/hitem3d/callback"
HITEM3D_CLIENT_ID = ""
HITEM3D_CLIENT_SECRET = ""

# -----------------------------------------------------------------------------
# E2E test helpers (only used for test endpoints)
# -----------------------------------------------------------------------------
E2E_TEST_TOKEN = ""
